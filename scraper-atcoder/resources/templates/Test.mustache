    it "input{{example}}" $ do
      input  <- BS.readFile "{{task}}/input{{example}}"
      expect <- BS.readFile "{{task}}/expect{{example}}"
      output <- captureIO M.main input
      prException output `shouldSatisfy` isNothing
      prStdout output `shouldBe` expect

